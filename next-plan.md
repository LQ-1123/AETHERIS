# 下一阶段目标：申请单（检查申请工作流）+ 管理员工作量报表

日期：2026-08-18
状态：已完成（2026-08-19）
前置：报告审核闭环已完成并通过手工验收（`515e3b1`，文档已归档 temp/completed-specs/）
目标：补完 8-18 拍板的业务流程（医院上传 → **技师写申请单** → 医生写报告 → 审核 → **管理员看工作量**/管理账号）中剩余的两块——申请单是流程起点，工作量报表是管理侧收尾。

完成记录：申请单领域模型、权限 API、独立技师页面、医生报告窗临床信息、签发联动、管理员工作量表与 CSV 导出均已实现；Rust 全工作区测试和 Viewer 构建/单测通过，Playwright 验收截图见 `temp/exam-request-acceptance/` 与 `temp/report-review-acceptance/`。

---

## 1. 现状盘点（已核实）

| 环节 | 状态 | 说明 |
| --- | --- | --- |
| 图像上传 | ✅ | DICOM C-STORE/STOW 接收；来源医院已提取 |
| 医生写报告 | ✅ | 报告工作台闭环（v0.2.0） |
| 审核 | ✅ | 审核人直接修正模式 + 错误计数（reviewer_modified） |
| 账号管理 | ✅ | 含密码重置审批 |
| **技师写申请单** | ❌ 全新 | 现有 `diagnostic_work_items`（0019 迁移）是按**序列**粒度的「接收工作项」（入库自动创建、来源归属用），**不是检查申请单**；无申请单概念、无技师录入界面 |
| **管理员工作量报表** | ❌ 全新 | 无聚合统计；数据源已就绪（见 §3） |

其他相关现状：
- `technician` 角色已存在（pacs-auth model.rs:20，权限列表 106 行）
- `list_reports`（pacs-web clinical.rs:937）仅按 study 查询，无按人/按时间聚合
- DIMSE 无 Modality Worklist（设备端 C-FIND MWL 拉取）——首期不做，列为可选增强
- 报告 5 态（draft/submitted/under_review/signed/amending）+ 审核事件表已就绪

---

## 2. 申请单（主交付）

### 2.1 领域模型（新表 `exam_requests`）

申请单是「先申请、后检查」（8-18 已拍板），申请时患者可能还没有 DICOM 入库记录，因此申请单**自带患者信息**，不与 patients 表强绑定：

```
exam_requests
├── id / institution_id（机构边界）
├── 患者信息：patient_id、patient_name、patient_birth_date?、patient_sex?
├── 检查请求：modality、body_part（部位）、request_type（平扫/增强…）、clinical_indication（临床指征/主诉）
├── 申请信息：requested_by（技师）、requested_at、scheduled_at?（预约时间，可选）
├── 状态：pending（待执行）→ executed（已执行）→ completed（已报告/已闭环）
├── 绑定：study_uid?（执行后关联的检查，可空）
└── created_at / updated_at
```

- 状态机建议：`pending → executed → completed`；`completed` 在报告签发（或审核完成）时由系统联动置位。
- 与 `diagnostic_work_items` 的关系：两者独立——work_items 管「序列来源归属」，exam_requests 管「临床申请」，不合并。

### 2.2 绑定机制（决策点 1）

申请单与入库检查（study）的绑定，三种方案：

| 方案 | 做法 | 优点 | 缺点 |
| --- | --- | --- | --- |
| 1. 申请单号写 DICOM | 申请单号写入检查的 AccessionNumber（0008,0050），入库时自动匹配 | 全自动 | 依赖设备/导入侧配合写入，老数据无 |
| 2. 患者ID+类型+日期匹配 | 入库时按患者ID+模态+日期模糊匹配 | 零配合 | 同患者同日同类检查有歧义 |
| 3. 技师手动绑定（推荐） | 申请单列表里，技师把已入库的检查手动关联到申请单 | 最可靠、无歧义 | 需要技师一步操作 |

**推荐：方案 3 为主（手动绑定，可靠无歧义），方案 1 作为后续可选增强**（设备端支持写入时再自动）。

### 2.3 界面

- **技师端**：新增「申请单」页面（队列页同级的独立页，或队列页内 tab）——申请单列表（按状态筛选）+ 新建申请表单（患者信息+检查请求+临床指征）+ 执行后手动绑定检查。
- **医生端**：报告工作台展示所写检查的申请单信息（临床指征辅助书写）；队列页检查行可标注「有申请单」。
- 入口：技师角色登录后可见申请单入口；医生可见申请单信息（只读）。

### 2.4 权限

- 创建/编辑：`technician`、`admin`（待执行态可改）
- 绑定检查：`technician`、`admin`
- 查看：`radiologist`（只读，临床指征）、`technician`、`admin`
- 机构边界沿用 `institution_id`

### 2.5 与报告流程衔接

- 申请单 `executed` 后，检查进入现有报告流程（队列页已按检查列出，报告闭环无需改动）。
- 报告签发/审核完成后，申请单自动置 `completed`（联动）。

---

## 3. 管理员工作量报表（辅交付）

### 3.1 数据源（全部现成，无需新表）

- 报告数：`diagnostic_reports`（author_fk、status、created_at/signed_at）
- 审核数：`report_review_events`（action='approved'、actor_fk）
- 错误计数：`report_review_events`（action='reviewer_modified'，按报告作者聚合）
- 申请单数（后续）：`exam_requests`（requested_by）

### 3.2 指标与维度（决策点 3）

- 时间范围：日 / 周 / 月（或起止日期）
- 按人分组：医生、技师
- 医生指标：报告数（草稿/送审/审核中/已签发）、被审核修改次数（错误数）、签发数
- 技师指标：申请单创建数（报表上线后可计）

### 3.3 界面

- 管理控制台新增「工作量」tab：时间范围选择 + 表格（用户 × 指标）+ 可选导出 CSV。

---

## 4. 决策点（需拍板）

| # | 决策点 | 推荐 |
| --- | --- | --- |
| 1 | 申请单与检查的绑定机制 | 方案 3 手动绑定为主，方案 1（申请单号写 DICOM）后续增强 |
| 2 | 申请单状态机是否与报告状态联动 | 是：报告签发/审核完成 → 申请单 completed |
| 3 | 工作量报表指标集合 | 按 §3.2（医生报告数/错误数/签发数 + 技师申请数） |
| 4 | 申请单界面位置 | 独立「申请单」页（与队列页平级），或队列页 tab——建议独立页 |
| 5 | 首期是否做设备 MWL 拉取 | 不做（可选增强） |

---

## 5. 工作量与顺序

| 阶段 | 内容 | 预估 |
| --- | --- | --- |
| 1 | 迁移 0031（exam_requests 表）+ 领域层 + API（CRUD/绑定/状态联动） | 1-1.5 天 |
| 2 | 技师申请单界面（列表/新建/绑定）+ 医生端只读展示 | 1-1.5 天 |
| 3 | 工作量报表（聚合查询 + 管理控制台 tab） | 1 天 |
| 合计 | | 约 3.5-4 天 |

顺序：申请单（迁移→API→界面）→ 工作量报表（可并行）。

## 6. 验收标准

1. 技师可创建申请单（患者/部位/模态/临床指征），列表按状态筛选。
2. 申请单可绑定已入库检查；绑定后医生在报告工作台看到临床指征。
3. 报告签发/审核完成后申请单自动 completed。
4. 权限：technician 可建；radiologist 只读；非相关机构不可见。
5. 工作量报表：按时间范围显示各医生报告数/审核数/错误数、技师申请数；admin 可见。
6. 回归：队列页/报告/审核流程不受影响（跑 cargo test + tsc）。

## 7. 不在本次范围

- 设备端 MWL（C-FIND）拉取申请单——可选增强
- C-MOVE/C-GET、AI 辅助报告、云胶片——沿用既有暂缓/候选结论
- 申请单与报告模板联动、批量申请——后续再说
